# Build and dependency output
node_modules/
.pnpm-store/
dist/
.turbo/
coverage/
pnpm-lock.yaml

# Next.js output (production build in .next, dev server in .next-dev - issue #54),
# generated ambient types, and Playwright output
.next/
.next-dev/
**/next-env.d.ts
apps/portal/.playwright/

# Generated API documents and static design showcases
docs/openapi/
plan/*.html
plan/**/*.html
plan/**/*.json
plan/**/*.css

# drizzle-kit generates these in a non-Prettier format (task 014 friction)
packages/db/migrations/meta/

# Vendored a2-react-aria component sources (task 028): upstream-owned, kept
# byte-for-byte (tabs/format) for a clean `a2ra diff` (ADR-22). Vitest snapshots
# are tool-generated.
packages/ui/src/components/a2ui/
**/__snapshots__/

# Chromium profile directories a Lighthouse run drops in the repository root under
# WSL (see `.gitignore` for why the name contains backslashes). Prettier walks the
# working tree rather than the index, so ignoring them in git alone still leaves
# `pnpm lint` failing on a few hundred `README` and `LOG` files after any browser
# run on this platform.
#
# Written without the backslashes git needs: Prettier's matcher does not read them
# as escapes the way gitignore does, and no path in this repository begins `C:`.
C:*
