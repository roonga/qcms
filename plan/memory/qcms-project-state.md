---
name: qcms-project-state
description: "Where qcms lives, execution status, and operational facts (updated 2026-07-30 - verify ledger/git before relying on task status)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9e5da939-93c1-4e50-a639-645e68acd50d
  modified: 2026-07-22T21:31:55.955Z
---

> **Read the dated updates below as an append-log, not as current fact** (noted 2026-08-01). Anything about `agent-loop.ps1`, a Windows host checkout, or a `.ps1` Windows fallback is superseded: ADR-29's 2026-07-25 amendment retired that supervisor outright and the dev container is the canonical seat. Ledger + `git log` win over every line here.

**Repos:** product repo github.com/roonga/qcms (local checkout beside its sibling repos; **PUBLIC since 2026-07-21**; pre-public audit passed: no secrets/PII in history, MIT LICENSE, SECURITY.md, branded README); plan archive: the retired `qcms-plan` checkout (superseded as source of truth by the repo's `docs/` the moment they diverge); component library: sibling checkout `../a2-react-aria` (github.com/roonga/a2-react-aria, `@a2ra/core` npm + shadcn-style vendored components).

**Status snapshot 2026-07-21:** backend complete through ~028 (kernel, db, api slices, renderer); **042 wireframes signed off (the Code Owner, 2026-07-21)** so the **UI phase is unblocked** - 029 (portal) is next, then 031-035 (admin). Also landed this day: repo public + hardened, static analysis (CodeQL + sonarjs + jscpd, #14), pre-040 security batch (#5/#6/#7/#8/#13), ADR-26 (UI stack), QCMS design tokens (theme.css). Open: follow-on #15 (submit endpoint scope). Executed by autonomous sessions via `/task`, `/next-task`, `/loop`, or `scripts/agent-loop.ps1`. Ledger = `docs/features/README.md` - always re-check it and `git log`, this snapshot ages. See [[qcms-design-system]].

**Update 2026-07-23:** UI phase in progress. 029 portal landed (SSR + strict BFF, design signed off 2026-07-22); 043 renamed the example domain to **vehicle insurance** (neutral, non-triggering); 044 no-JS submission done. **030 portal a11y** in-progress; its manual NVDA/VoiceOver pass is the Code Owner's human gate and is now **BLOCKED on 045**. New ADRs: **ADR-27** (i18n: no user-facing string ever hardcoded, both apps, locale-aware `Intl`), **ADR-28** (portal step navigation: explicit Continue/Back/Submit, no collapse-on-answer). **Task 045** (in flight, Stage-7 blocker, ordering exception added to next-task skill): fixes the portal flow - the shipped derived-cursor model (`currentStep` = first step with an unanswered required, `evaluate-rules.ts:388-414`) broke multi-choice (collapsed after one pick) and regressed submit; 045 adds explicit nav + a **full-stack** kitchen-sink e2e (phone/tablet/desktop, an **independent Postgres** query verifying persisted answers match input + append-only + submission lock, and browser + API/DB **container-log** error gates). Manual portal review 2026-07-23 produced findings **A-N** (see [[qcms-open-decisions]]). Created **`docs/AUDIT_AGENT.md`**: charter for an independent, adversarial, read-only verifier (project-goal/security/hygiene/docs-vs-git; runs before 038 and inside 040; reports evidence, never fixes). **Dev ports are 7xxx** (portal 7000, API 7010, Postgres 7020); `pnpm dev:portal` seeds+publishes a kitchen-sink form and serves it. the Code Owner keeps a **live dev session** open - `/task NNN` there is the direct launch route; `agent-loop.ps1` (needs `pwsh`) is only for unattended looping across usage windows.

**Devcontainer (ADR-29, settled 2026-07-23):** adopt a Dev Container (Ubuntu 24.04 image + Features incl. `docker-outside-of-docker` for Testcontainers) as the canonical env; loop runs inside with `bypassPermissions` (container = blast radius); canonical supervisor becomes `agent-loop.sh` (`.ps1` kept for Windows fallback); cross-platform guards stay; dev-loop repos move to WSL2 fs (PO seat stays on Windows checkout). Decisions A-D all confirmed. **Task 046** implements it, **runs after 045** (045's e2e is 046's acceptance test - must pass *inside* the container); dispatch manually via `/task 046`, not auto-selected. Additive + reversible (host `pwsh agent-loop.ps1` remains). Original plan in scratchpad artifact.

**Plan shape:** 50+ tasks (043-046, 048-056 added out of sequence); ADR-01…35 (16–35 in `docs/PROJECT_GOAL.md`); SEC-1…13. **Ordering exceptions live in exactly one place - the table in `docs/features/README.md` - and are never copied here**; the list this line used to carry (040/041/042/043/045) was one of five stale copies purged on 2026-08-01, and exceptions retire when their task lands. Wireframes in `docs/wireframes/` (signed off at 042, 2026-07-21; portal-flow re-signed 2026-07-30 for 053's brand/appearance regions). Naming settled: `@qcms/*` packages, `qcms-api/portal/admin` apps, `create-qcms-app` (all npm-free as of 2026-07-19; org creation before Stage 5 publish).

**Workshop:** executor+reviewer subagents, FRICTION retro log (`docs/RETRO.md`) consumed by `/improve-workshop` at stage boundaries; screenshot gates for UI tasks. Scope v2 doc: the dangling references were **removed** 2026-07-21 (R1–R7 now canonical in `PROJECT_INSTRUCTIONS.md`); re-add a `docs/SCOPE.md` if/when the Code Owner supplies the original.

**Static-analysis division (settled 2026-07-21):** CodeQL (`.github/workflows/codeql.yml`, `security-and-quality` suite) owns **security SAST** server-side; local `sonarjs + jscpd` (still stashed, ~106-finding triage pending) owns **code quality + duplication** at write-time in the `pnpm lint` gate. GitHub "Code quality findings" deliberately left **disabled** - it duplicates sonarjs on generic smells, does nothing for jscpd-style duplication, and only detects after push rather than blocking at write-time. Public-repo security features all on: secret scanning + push protection, Dependabot alerts + auto security fixes, private vulnerability reporting.

**Update 2026-07-26:** the dev loop and this seat interlock via the PR-per-issue flow (see [[qcms-pr-flow]]). Landed across 2026-07-25/26: ~24 issues via reviewed PRs; `pnpm verify` one-command gate + `check:changeset` (#83); GHCR image mirror for CI Postgres (#78; ACTIVE since 2026-07-26 - mirror workflow ran, package publicly pullable, verified by anonymous manifest fetch); ADR-31 (commitment semantics, PR #90) / ADR-32 (validation messages, task 048) / ADR-30 amendment (theme editor launch tier, task 049) / ADR-33 (retraction tombstone, task 050 DONE via PR #97); component guidelines doc; CI checks renamed `verify (node-NN)`. 030 manual a11y pass still the next human gate on the critical path; 047 theming is the next feature task.

**Update 2026-07-30:** big landing day (2026-07-29/30). **ADR-34 + task 054 observability DONE (PR #181)**: OTel tracing portal -> API -> pg via official libraries only (`NodeSDK`, `@hono/otel`, `@vercel/otel`, `instrumentation-pino`), pino-backed Logger, SEC-13 allowlist redaction row, hard no-op without `OTEL_EXPORTER_OTLP_ENDPOINT`; this seat ran the whole /task flow itself on the Code Owner's direct order (author ADR + task file, executor/reviewer subagents, PR, merge). Theming train: **051 DONE (PR #190)** and **052 DONE (PR #194)** - both merged by the dev conductor itself (its /task flow lands its own task PRs; this seat's role there is stranger-review + sentinel, which crossed benignly with the conductor merge both times); **053 PR #198 PARKED at the Code Owner's two gates** (24 screenshots under `docs/gates/053/` + wireframe re-sign for the new brand/appearance Regions and States) - CI green, both reviewers clean, closes 047 when signed and merged. jsdom majors: PR #176 (26 -> 30) closed onto #113 (same 8 conformance failures as #111). Conformance validations run per Code Owner directive: **zod-openapi in sync** with the official guide except missing `defaultHook` (filed #182, raw ZodError 400s bypass the ErrorEnvelope); **better-auth validation queued for 031's PR**. New issues this run: #182-#189, #192, #195-#197. 031 admin shell still in-progress dev-side (no branch pushed yet). Awaiting Code Owner: 053 gates, 030 manual a11y pass, #168 posting-policy one-liner, 055 call (serve OpenAPI + Scalar UI - proposed, no migration to hono-openapi). Ground rule added 2026-07-29: plan against official docs, not memory (see plan/CLAUDE.md).

**Update 2026-07-30 (evening):** theming train COMPLETE - 053 merged (PR #198) after the Code Owner cleared both gates (screenshots + wireframe re-sign, recorded in portal-flow.md), closing 047. Cobalt admin theme designed and landed (PR #199): `plan/admin-theme/` holds build.mjs (WCAG gate), tokens.css, spec, preview; also pushed to the QCMS Design System Claude Design project (admin/theme.html card + foundations/admin-theme-tokens.css). A draft task file (admin theme application, slot after 031 before 032) sits in `plan/admin-theme/` awaiting the Code Owner's nod to land in docs/features/. Artifacts HTTP server landed (PR #200): `pnpm artifacts` on port 7030 serves docs/gates, plan previews, and worktree gate evidence read-only; container rebuild needed to publish the port. **055 (serve OpenAPI + docs UI) PARKED by the Code Owner** - no runtime serving for now, Scalar judged too heavy; recorded as issue #201 (phase-4). Still awaiting: 030 manual a11y pass, #168 posting-policy line, two-merger coordination rule, the admin-theme task-file landing nod. 031 still in dev.

**Update 2026-08-22 (the admin redesign campaign, which the entries above predate entirely).** Everything from 2026-07-31 on is missing from this log, so a session booting on the entries above will not know the last three weeks happened. In brief, and **verify against the ledger and `git log` as always**:

- **Stage 8a is done.** 031-035 all landed, as did 047 (via 051/052/053), 048, 050, 054, 055 and 057. **54 of 62 tasks are `done`.** Remaining `todo`: **049**, **061**, **037**. **041 never gates 038** (flag-gated, ADR-25); **039** is Stage 9.
- **The launch path is three implementation tasks and three human gates.** The gates: **030** manual screen-reader pass, **040** security sign-off (implementation **merged 2026-08-16**, PR #495, exit criterion 3 met - it waits on a signature rather than on work), and **038** itself.
- **An `admin-redesign` tier ran 2026-08-19 to 2026-08-22 and its authorised scope is discharged.** All twelve original issues (#510-#515, #517-#522) are closed. Roughly twenty remain under the label, **none from the original set** - they are all second-generation, found while draining the first. Whether that label continues is an open Code Owner decision, recorded in [[qcms-open-decisions]].
- **`plan/admin-design-contracts.md` is the tiebreak for admin UI**, confirmed by the Code Owner 2026-08-20 over eight contracts, with a ninth (§9, unbounded author text) and several amendments open at the time of writing. **The eleven POCs in `plan/admin-shell-poc/` are not the reference** - they answer the same questions two to seven different ways, which is why the contracts exist.
- **The admin screenshot gate is delegated to this seat** for contract-governed work (Code Owner ruling 2026-08-20). Escalate anyway when a render contradicts a contract, when nothing governs it, when it introduces a new pattern, or when it touches the **portal**. Every other human gate is untouched.
- **Operationally:** both seats now run a 10-minute in-session duty-cycle job, because seat mail is only read at the top of a loop iteration and a stopped loop therefore cannot be told to restart by mail (#597). `scripts/agent-loop.sh` has not actually supervised the dev seat since 2026-08-01.
- **The lesson worth carrying** (#598): six times in three days, across both seats, a **right conclusion rested on evidence that was never checked**. None was caught by review or by any gate; each was caught by someone re-reading a source while writing something else, and twice by an executor refusing a brief. Two rules came out of it: quote or line-reference the file at the moment of writing, **and** check a claim about a set against the whole set or state which part you read. Related: the repo-root checkout is stale by design, so `git show origin/main:<path>` rather than a bare read.
