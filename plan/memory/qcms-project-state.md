---
name: qcms-project-state
description: "Where qcms lives, execution status, and operational facts (snapshot 2026-07-23 — verify ledger/git before relying on task status)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 9e5da939-93c1-4e50-a639-645e68acd50d
  modified: 2026-07-22T21:31:55.955Z
---

**Repos:** product `H:\source\agent3\qcms` (github.com/roonga/qcms — **PUBLIC since 2026-07-21**; pre-public audit passed: no secrets/PII in history, MIT LICENSE, SECURITY.md, branded README); plan archive `H:\source\agent3\qcms-plan` (superseded as source of truth by the repo's `docs/` the moment they diverge); component library `H:\source\agent3\a2-react-aria` (github.com/roonga/a2-react-aria, `@a2ra/core` npm + shadcn-style vendored components).

**Status snapshot 2026-07-21:** backend complete through ~028 (kernel, db, api slices, renderer); **042 wireframes signed off (Ravi, 2026-07-21)** so the **UI phase is unblocked** - 029 (portal) is next, then 031-035 (admin). Also landed this day: repo public + hardened, static analysis (CodeQL + sonarjs + jscpd, #14), pre-040 security batch (#5/#6/#7/#8/#13), ADR-26 (UI stack), QCMS design tokens (theme.css). Open: follow-on #15 (submit endpoint scope). Executed by autonomous sessions via `/task`, `/next-task`, `/loop`, or `scripts/agent-loop.ps1`. Ledger = `docs/features/README.md` - always re-check it and `git log`, this snapshot ages. See [[qcms-design-system]].

**Update 2026-07-23:** UI phase in progress. 029 portal landed (SSR + strict BFF, design signed off 2026-07-22); 043 renamed the example domain to **vehicle insurance** (neutral, non-triggering); 044 no-JS submission done. **030 portal a11y** in-progress; its manual NVDA/VoiceOver pass is Ravi's human gate and is now **BLOCKED on 045**. New ADRs: **ADR-27** (i18n: no user-facing string ever hardcoded, both apps, locale-aware `Intl`), **ADR-28** (portal step navigation: explicit Continue/Back/Submit, no collapse-on-answer). **Task 045** (in flight, Stage-7 blocker, ordering exception added to next-task skill): fixes the portal flow - the shipped derived-cursor model (`currentStep` = first step with an unanswered required, `evaluate-rules.ts:388-414`) broke multi-choice (collapsed after one pick) and regressed submit; 045 adds explicit nav + a **full-stack** kitchen-sink e2e (phone/tablet/desktop, an **independent Postgres** query verifying persisted answers match input + append-only + submission lock, and browser + API/DB **container-log** error gates). Manual portal review 2026-07-23 produced findings **A-N** (see [[qcms-open-decisions]]). Created **`docs/AUDIT_AGENT.md`**: charter for an independent, adversarial, read-only verifier (project-goal/security/hygiene/docs-vs-git; runs before 038 and inside 040; reports evidence, never fixes). **Dev ports are 7xxx** (portal 7000, API 7010, Postgres 7020); `pnpm dev:portal` seeds+publishes a kitchen-sink form and serves it. Ravi keeps a **live dev session** open - `/task NNN` there is the direct launch route; `agent-loop.ps1` (needs `pwsh`) is only for unattended looping across usage windows.

**Devcontainer (ADR-29, settled 2026-07-23):** adopt a Dev Container (Ubuntu 24.04 image + Features incl. `docker-outside-of-docker` for Testcontainers) as the canonical env; loop runs inside with `bypassPermissions` (container = blast radius); canonical supervisor becomes `agent-loop.sh` (`.ps1` kept for Windows fallback); cross-platform guards stay; dev-loop repos move to WSL2 fs (PO seat stays on Windows checkout). Decisions A-D all confirmed. **Task 046** implements it, **runs after 045** (045's e2e is 046's acceptance test - must pass *inside* the container); dispatch manually via `/task 046`, not auto-selected. Additive + reversible (host `pwsh agent-loop.ps1` remains). Original plan in scratchpad artifact.

**Plan shape:** 42+ tasks (043/044/045/046 added out of sequence) + 040/041/042/043/045 ordering exceptions; ADR-01…29 (16–29 in `docs/PROJECT_GOAL.md`); SEC-1…12; wireframes pre-drafted in `docs/wireframes/` (Draft pre-027, my sign-off pending at 042). Naming settled: `@qcms/*` packages, `qcms-api/portal/admin` apps, `create-qcms-app` (all npm-free as of 2026-07-19; org creation before Stage 5 publish).

**Workshop:** executor+reviewer subagents, FRICTION retro log (`docs/RETRO.md`) consumed by `/improve-workshop` at stage boundaries; screenshot gates for UI tasks. Scope v2 doc: the dangling references were **removed** 2026-07-21 (R1–R7 now canonical in `PROJECT_INSTRUCTIONS.md`); re-add a `docs/SCOPE.md` if/when Ravi supplies the original.

**Static-analysis division (settled 2026-07-21):** CodeQL (`.github/workflows/codeql.yml`, `security-and-quality` suite) owns **security SAST** server-side; local `sonarjs + jscpd` (still stashed, ~106-finding triage pending) owns **code quality + duplication** at write-time in the `pnpm lint` gate. GitHub "Code quality findings" deliberately left **disabled** — it duplicates sonarjs on generic smells, does nothing for jscpd-style duplication, and only detects after push rather than blocking at write-time. Public-repo security features all on: secret scanning + push protection, Dependabot alerts + auto security fixes, private vulnerability reporting.
