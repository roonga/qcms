# QCMS portal review findings - 2026-07-23

Running log from the manual/automated portal review (Ravi driving, PO capturing). Concrete bugs/gaps become GitHub issues when the review closes; design questions become ADR/phase-4 tickets. Severity: S1 blocker, S2 confusing/non-conforming, S3 polish.

| # | Finding | Type | Sev | Owner | Proposed resolution |
|---|---|---|---|---|---|
| A | **CSP-nonce hydration mismatch** on load: SSR renders `nonce=""` on a `<head>` script, client hydrates a real nonce. Breaks React hydration + undermines the CSP nonce; prime suspect for the flaky portal-e2e. | bug | S2 | portal (029) | Propagate the per-request CSP nonce into SSR so server + client match; then hydration is clean and the CSP nonce actually enforces. |
| B | **No console-error / hydration gate**: the portal e2e never fails on a browser `console.error`/`pageerror`/hydration warning, so A-class bugs land green. | process | S2 | workshop | Add `page.on("console"/"pageerror")` zero-error assertions to the portal e2e. Folds into **#19** (local gate not a superset of CI). |
| C | **Error-summary links do not identify the field**: all missing-required errors render the same string ("This question needs an answer") so the summary links have identical accessible names. WCAG 3.3.1. | bug | S2 | portal | Prefix each summary link with the question label ("Full name - ...") -> `#q_...`. |
| D | **Custom error messages** not authorable in the schema (messages are system-generated + shell i18n). | design | - | schema | ADR-level decision (DSL minimalism vs author flexibility vs immutability/i18n cost). Likely Phase-4. |
| E | **No e2e/axe coverage for the kitchen-sink form**: the portal e2e drives only the 2-question insurance/vehicle form, so the 7 question-type renderers (date, multi-choice, etc.) are never browser- or axe-tested. | coverage | S2 | e2e | Add a kitchen-sink e2e path (all types) to the axe/keyboard/lighthouse suites. This is the a11y-automation gap. |
| F | **i18n discipline** - no hardcoded user-facing text, both apps, locale-aware Intl. | done | - | - | Landed as **ADR-27** (9696766). |
| G | **Back button missing**: step flow renders only Continue; signed wireframe specifies a Back (secondary; absent on first step). | bug | S2 | portal | Add the Back control on non-first steps per the wireframe; review append-only semantics of revisiting an answered step. |
| H | **Date input needs admin options**: (1) input mode `datepicker + keyboard` vs `keyboard-only`; (2) segment auto-advance is react-aria default - consider a toggle (WCAG 3.2.2 angle). | feature/config | - | schema + builder (033) | Extend the date question type in the DSL with an input-mode option; surface in the builder. ADR-worthy (closed DSL). |
| I | **Form multi-language authoring UX not built**: schema supports multi-language content (LocalizedText/LocaleCode/defaultLocale, ADR-11) and the renderer honors locale, but there is no admin UI to enter translations. | gap | - | admin (031/032) | Multi-locale input fields in the question library / builder. Runtime respondent locale-switcher stays Phase-4 (ADR-27). |
| J | **Portal header brand hardcoded**: `<span>QCMS</span>` in `portal-shell.tsx` (and `title="QCMS"` in page.tsx) - only changeable by editing source. | bug/config | S2 | portal shell | Drive the brand mark (text + optional logo) from adopter config (env / theme), not a source literal. Aligns with ADR-27 + adopter-themeable (ADR-26). |

| K | **Managed theming**: ship pre-defined themes (so an admin doesn't start blank), let them customize a predefined theme's tokens, and **save it as a named custom theme** that persists. | feature (large) | - | schema/db + admin + portal + design | New ADR (extends ADR-22/26) + a task set - see "Proposed new work" below. Scope + granularity decision needed. |

| M | **Multi-choice / checkbox unusable**: ticking one checkbox auto-submits before you can pick more. Cause: `step-flow.tsx:245` `if (readyToSubmit) void submit()` auto-submits the moment a selection completes the required set (choices post immediately, line 202). Same behavior as H, but here it is an outright bug - multi-select is impossible when the question completes the form. Also **WCAG 3.2.2 (On Input)**. | bug | **S1** | portal | Remove auto-submit-on-readyToSubmit; require an explicit Submit click. Fold H's "auto-advance" into an admin option that defaults **off**. This one fix resolves H + M. |
| N | **Submit on the last step goes back a step**: clicking Submit on step 3 returns to step 2 instead of completing. Flow-state / step-index bug in the submit path (`step-flow.tsx` submit handler + serve-step re-render). Likely same family as M/G - the flow-state machine mishandles the terminal transition. | bug | **S1** | portal | Trace the submit -> serve-step response: confirm `flowState.stepId`/`readyToSubmit` after a valid final submit; add an e2e that completes the full kitchen-sink flow and asserts the completion screen (ties to E + L). |
| L | **e2e runs one viewport only** (`mobile-chromium`): nothing verified at tablet or desktop widths, despite the mobile-first-but-responsive mandate. | coverage | S2 | e2e (030) | Add Playwright projects for **phone (~390), tablet (~768), desktop (~1280)** and run the flow + axe/keyboard/lighthouse suites across all three. Pairs with E (also cover kitchen-sink). |

## Proposed new work: managed theming (from finding K + J)

This shifts theming from ADR-22's "adopter edits token values in the scaffolded shell source" to a **managed model**: predefined themes, a customization UI, and stored named custom themes. That is a real product-direction change and needs an **ADR** before task files. Rough shape:

- **ADR-28 (proposed):** managed themes - a theme is a named set of token values (the shadcn/a2ra tokens, ADR-27's brand/logo text included); QCMS ships N predefined themes; an operator/admin can fork one, customize tokens, and save a named custom theme; the active theme is applied to the portal (and admin chrome). Decide: stored where (DB row vs config file), granularity (per-deployment vs per-form vs per-org), and whether this is **launch or Phase-4**.
- **Tasks (draft):** (1) define the predefined theme token sets (design-system output: QCMS default Cobalt + a small set of alternates); (2) theme storage in `@qcms/db` (name + token values, immutability/versioning stance); (3) admin theme-management UI (browse predefined -> customize tokens + brand text/logo -> save named); (4) apply the selected theme at the portal (and admin) shell.

**Open decisions for Ravi:** launch vs Phase-4? per-deployment or per-form themes? does a saved theme need versioning/immutability like published forms, or is it mutable operator config? These shape the ADR.

**Not-yet-filed:** all rows except F. When the review closes I will file A/C/G/J as bugs, E as a coverage task, B into #19, D/H/I as design/config tickets, and post a one-line summary.

**Baseline (for balance):** the portal a11y is otherwise strong - skip link, `banner`/`main` landmarks, `h1->h2` hierarchy, labeled+described inputs, "Step N of M" polite live region, and a focused assertive error summary with per-field anchors. This review is finding the edges, not a weak foundation.
