# CI fast-lane probe

Throwaway file. It exists to prove that a pull request whose whole diff sits under
plan/ takes the fast lane and that all four required contexts still report. Deleted
once the evidence is in PR #525.
concurrency demo push 1
concurrency demo push 2
