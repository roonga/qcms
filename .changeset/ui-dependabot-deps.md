---
"@roonga/qcms-ui": patch
---

Dependency maintenance (Dependabot grouped minor/patch): `dom-accessibility-api`
`^0.6.3` to `^0.7.1`, and the `react`/`react-dom` dev/peer ranges to `^19.2.8`.
No API or behavior change in `@roonga/qcms-ui` itself; consumers resolve newer
in-range versions of these dependencies.
