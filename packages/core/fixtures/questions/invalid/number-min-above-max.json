{
  "description": "number min must not exceed max",
  "expected": { "code": "MIN_ABOVE_MAX", "path": ["constraints", "min"] },
  "definition": {
    "type": "number",
    "questionId": "q_accident_count",
    "label": { "en": "How many?" },
    "constraints": { "min": 10, "max": 1 }
  }
}
