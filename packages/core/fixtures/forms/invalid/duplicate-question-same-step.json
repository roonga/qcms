{
  "description": "q_at_fault_accident is pinned twice within one step; the uniqueness refinement is form-wide, so same-step duplicates are caught too (DUPLICATE_QUESTION_IN_FORM).",
  "expected": {
    "code": "DUPLICATE_QUESTION_IN_FORM",
    "path": ["steps", 0, "items", 1, "questionId"]
  },
  "definition": {
    "formId": "frm_auto_quote",
    "defaultLocale": "en",
    "title": { "en": "Vehicle insurance quote" },
    "steps": [
      {
        "stepId": "stp_history",
        "title": { "en": "Driving history" },
        "items": [
          { "questionId": "q_at_fault_accident", "version": 2 },
          { "questionId": "q_at_fault_accident", "version": 2 }
        ]
      }
    ],
    "rules": []
  }
}
