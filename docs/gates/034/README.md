Approve the task 034 publish, preview, version-history and secure-link screens: the publish confirmation stating what freezes and what happens to in-flight sessions (`publish-confirm`), a refused publish's anchored issue list with nothing persisted (`publish-rejected`), the version badge and history link after a successful publish (`publish-success`), the live draft preview with its "Preview - not published" banner (`preview`) and the same step with a branch revealed (`preview-branch`), the published-version table with its compiler / A2UI-spec / semantics stamps (`version-history`), one frozen version rendered from its stored compiled documents (`version-view`), the secure-link screen before any link exists (`links-empty`), the mint dialog with expiry, one-time and batch count (`links-mint`), the one-time panel of minted URLs with copy and CSV export (`links-minted`), and the lifecycle table showing a revoked link's chip (`links-revoked`) - each at 390px and 1280px in light, dark and high contrast. Timestamps render locale-aware in UTC with the zone named (ADR-27); the CSV export keeps ISO, which is a machine artifact. The Consumed and Expired chips are not in this set: reaching them needs a respondent session and a clock this suite does not have.
