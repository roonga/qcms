Reference evidence for issue #510, not a blocking gate: nothing waits on a sign-off here, and the tier's gated issues are #514, #515, #517, #518, #519 and #522. The frames show the two detail routes now headed by their own entity rather than by the form's slug - `response-detail` (`<h1>` reads `Response ses_…`, with the form left in the breadcrumb and the id no longer repeated as an `<h2>` below) and `version-detail` (`<h1>` reads `Version 1`, with the body's old "Viewing v1" heading gone) - each at 390px and 1280px in light mode, since what changed is which words a heading holds and that is identical in all three modes. Shot by `apps/admin/e2e/gate-screenshots-510.pw.ts` with `QCMS_ADMIN_CAPTURE_GATE=1`.
