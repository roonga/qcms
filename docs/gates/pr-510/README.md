Screenshot evidence for issue #510, for the Code Owner to review on the PR: the two admin detail routes are now headed by their own entity rather than by the form's slug. `response-detail` - the `<h1>` reads `Response ses_…` (previously the form's slug), the form has moved into the breadcrumb, and the session id is no longer repeated as an `<h2>` below. `version-detail` - the `<h1>` reads `Version 1` (previously the form's slug) and the body's old "Viewing v1" heading is gone. Each route at 390px and 1280px in light mode, since what changed is which words a heading holds and that is identical in all three modes; at 390px the response `<h1>` wraps its `ses_…` token across three lines. Shot by `apps/admin/e2e/gate-screenshots-510.pw.ts` with `QCMS_ADMIN_CAPTURE_GATE=1`.
