Approve the task 041 agent panel docked beside the form builder: the empty panel with its "describe the form you want" prompt (`assist-empty`), a completed proposal showing the grouped diff of what it adds or changes against the current draft and the server's own validation line (`assist-proposal`), the same proposal with one diff entry expanded to its full definition (`assist-proposal-expanded`), the draft after Accept into draft with the "includes agent-assisted changes" provenance mark on the builder (`assist-accepted`), and a turn refused because the model reached for a tool outside the server-side allowlist (`assist-refused`) - each at 390px and 1280px in light, dark and high contrast. The panel deliberately offers no publish, erase, link or webhook affordance of any kind: the allowlist is enforced server-side and the UI mirrors it by never rendering those actions. Every screenshot is the deterministic fake provider, which drives the shipped tool loop with a scripted model rather than a vendor call.
