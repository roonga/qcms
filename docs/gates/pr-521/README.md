# Gate evidence - issue #521

Approve the one new element and the one moved sentence this change puts on the form-scoped response browser: a warning notice naming filter values the address carried that no filter accepts, and which empty sentence sits below it. `ignored-filters-*` is `?flagged=maybe&from=nope`, where nothing parses, so the notice names both parameters and the empty state reads "Nothing has been submitted to this form yet." (it used to claim no response matched filters the page had discarded); `ignored-filters-with-valid-*` is `?flagged=true&from=nope`, where one filter survives, so the singular copy names only the rejected one and the empty state correctly stays "No response matches these filters." Both states in light, dark and high-contrast, at 390px and 1280px.
