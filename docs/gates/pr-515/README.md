# Gate: the responses table's answer-preview column (issue 515)

Approve the sixth column the wireframe specifies: at 1280 the answer preview is present and truncated; at 390 it is the one column that drops, leaving the five that shipped before. Answers shown are invented fixture values, not respondent data.
