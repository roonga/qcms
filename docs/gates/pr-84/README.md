# Gate evidence - PR #84 (issue #20)

Approve that the portal renders exactly as before this change: the CSP-nonce work is behaviour-preserving and invisible, so these are the unchanged loaded entry screen at phone and desktop widths (`nonce-entry-390.png`, `nonce-entry-1280.png`) captured with the dev overlay removed.
