Approve the task 035 response, erasure and webhook-operations screens: the response browser with its version / date / flagged filters and per-row link into the detail (`responses-browser`), a filtered result that matches nothing and says so in those words rather than "nothing has been submitted" (`responses-filtered-empty`), one response with its locked answers captioned by the pinned question version and its append-only ledger timeline (`response-detail`), the export dialog with CSV chosen and no version yet, so the "a version is required" hint is showing, Download is inert, and the empty-result sentence describes a CSV specifically rather than both formats at once (`export-dialog`), the type-to-confirm erasure dialog stating the three ADR-17 facts with the session id typed and the destructive button now enabled (`erase-confirm`, whose third paragraph is the sentence the PO review of #284 rejected and now says that an event for this session still waiting to be delivered is **not** withdrawn by erasing and may still be sent, with redelivery from the dead-letter queue refused), the tombstone that replaces the response afterwards, carrying the operator-facing form of the reason rather than the raw enum (`tombstone`), the erasure log as compliance evidence, reading the same way (`erasure-log`), webhook configuration before any endpoint exists, shot on the seeded kitchen-sink form because the insurance form used for every other frame has one configured by then (`webhooks-none`), the one-time secret reveal that says it will not be shown again (`secret-reveal`), the endpoints table showing "Stored, not retrievable" where a secret would be (`webhooks-table`), the delivery dashboard with one delivery's request headers open and the signature masked (`delivery-detail`), the dead-letter queue with its per-item and bulk redeliver controls (`dead-letters`), and the same queue reporting a redelivery as queued for the next pass rather than as delivered (`redeliver-queued`) - each at 390px and 1280px in light, dark and high contrast, named `<state>-<mode>-<viewport>` as in the 033 and 034 sets. The secret shown in `secret-reveal` is a placeholder the capture spec substitutes before the shutter, so no committed frame carries a minted value. Every timestamp renders locale-aware in UTC with the zone named (ADR-27). Two states from the wireframe's inventory are **not** in this set and were not reached: "no responses" (a form that has collected nothing renders the same component as `responses-filtered-empty` with one different sentence, and reaching it needs a second form authored only for the frame) and "flagged present" (a flagged submission comes from the honeypot or minimum-time path in 020/026 and cannot be provoked from these routes).
