Approve the task 059 erase-dialog copy: the type-to-confirm erasure dialog with the session id typed and the destructive button enabled (`erase-confirm`), whose third ADR-17 paragraph replaces 035's pre-059 statement - it no longer says an undelivered event "is not withdrawn by this action: it may still be sent", and now states the three post-059 facts separately: an event already delivered stays delivered and that copy is the consumer's to erase as an independent data controller, an event for this session that has not been delivered is cancelled and will never be sent, and QCMS's own stored copy of the answers in the queued event is redacted - each at 390px and 1280px in light, dark and high contrast, named `<state>-<mode>-<viewport>` as in the 033, 034 and 035 sets. This is the only photographed surface 059 changes, so the rest of `docs/gates/035/` still stands; the delivery dashboard's new `cancelled` badge and its reason line are asserted in `apps/admin/e2e/responses-ops.pw.ts` rather than photographed, because reaching that state in a capture needs the whole 035 delivery arc per mode to show one tag.
