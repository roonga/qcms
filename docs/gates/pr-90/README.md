# Gate evidence - PR #90 (issue #31, ADR-31)

Approve that answers now commit at each control's ADR-31 moment, at phone (390) and desktop (1280) widths, with the dev overlay removed. A screenshot cannot show timing, so each pair is the state that only the new cadence produces: `multichoice-uncommitted-*` (two options chosen, focus still in the group, the question they gate deliberately NOT yet revealed) against `multichoice-committed-on-group-exit-*` (the same screen once focus leaves the group: one post, one reveal); `boolean-revealed-on-change-*` and `singlechoice-committed-on-change-*` (the follow-up and the ready state reached from the click alone, no blur, which is the issue #31 fix); and `date-partial-no-error-*` (a half-entered date sits quietly instead of posting `null` and flashing "invalid value").

Open question inside this approval: ADR-31's date row says "on completion (all segments filled)", but the react-aria DatePicker raises that signal on every digit typed into the year, so `1990` emits the complete dates 0001, 0019, 0199 and 1990. The date therefore commits when editing ends with all segments filled, not at the control's own completeness signal. Confirm or amend ADR-31's date row.
