{
  "name": "qcms-portal",
  "version": "0.0.0",
  "private": true,
  "description": "qcms respondent portal (Next.js) (placeholder, task 001)",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --root ../.. --project qcms-portal",
    "lint": "eslint src"
  }
}
