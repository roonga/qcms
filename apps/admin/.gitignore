# Next.js
# Production build output (`next build` / `next start`) and, kept deliberately
# separate from it, the dev server's own output (issue #54).
/.next/
/.next-dev/
/next-env.d.ts

# TypeScript incremental build info
*.tsbuildinfo

# Playwright
/.playwright/

# Captured static-render screenshots (screenshot-gate artifacts, not source)
/screenshots/
