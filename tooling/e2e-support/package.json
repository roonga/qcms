{
  "name": "@qcms/e2e-support",
  "version": "0.0.0",
  "private": true,
  "description": "Browser-suite helpers both QCMS frontends share, so a hardening lands once (issues 187, 236)",
  "license": "MIT",
  "type": "module",
  "exports": {
    "./animations": "./src/animations.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
